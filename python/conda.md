## Use PIP to install something inside conda environment

The only thing needed is to use pip installed inside virtual environment (`<path_to_venv>/bin/pip`). If you use pip, this call references anaconda pip.

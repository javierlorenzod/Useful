1. Change default login shell: command `chsh` (Change shell)
```
$ chsh
  Contraseña: 
  Cambiando la consola de acceso para jld
  Introduzca el nuevo valor, o presione INTRO para el predeterminado
	  Consola de acceso [/bin/zsh]: /bin/zsh
$
```
